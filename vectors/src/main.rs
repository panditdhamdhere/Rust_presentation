fn main() {
    println!("Hello, vectors!");


    // statically
    let mut abc = vec![1, 2, 3, 4, 5];
    println!("v: {:?}", abc);

    abc.push(12);
    println!("v: {:?}", abc);
}


// vectors we use to store multiple values in a single variable -> same like arry and variables
